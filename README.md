# Fort Greene Press

The website for Fort Greene Press, published with GitHub Pages.

## Development

Open `index.html` directly, or run a local static server:

```sh
python3 -m http.server 8000
```

Every push to `main` deploys the site through GitHub Actions.
